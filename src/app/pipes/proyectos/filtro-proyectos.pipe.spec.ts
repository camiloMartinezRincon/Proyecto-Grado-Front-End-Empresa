import { FiltroProyectosPipe } from './filtro-proyectos.pipe';

describe('FiltroProyectosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProyectosPipe();
    expect(pipe).toBeTruthy();
  });
});
